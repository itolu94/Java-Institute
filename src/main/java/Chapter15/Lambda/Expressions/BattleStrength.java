package main.java.Chapter15.Lambda.Expressions;

//need an functional interface (interface with a abstract method) to create a lambda function
public interface BattleStrength {
    double calculateStrength(int age, double weight);
}
