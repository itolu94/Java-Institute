package main.java.Chapter4.Operators;

public class OperatorPrecedene {
    public static void main(String args[]){
        /*
        Highest
            (++ postfix), (-- postfix)
            (++ prefix), (--prefix)
            (*), (/), (%),
            (+), (-)
            (>>), (>>>), (<<)
            (>), (>=), (<), (<=), instanceof
            (==), (!=)
            (&)
            (^)
            (|)
            (&&)
            (||)
            (?:)
            (->)
            (=), (op=)
        Lowest

        - "Parentheses raise the precedence of the operations that are inside them"
         */
    }
}
