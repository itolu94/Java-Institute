package main.java.Chapter4.Operators;

public class RelationalOperators {
    public static void main(String args[]) {
        /*
            - Determine equality and ordering
                1. (==)   -> Equal to
                2. (!=)   -> Not equal
                3. (>)    -> Greater than
                4. (<)    -> Less than
                5. (>=)   -> Greater than or equal
                6. (<=)   -> Less than or equal

             -  outcomes are boolean values
             -  Only Integer types can use ordering (>, <, >=, <=) comparisons
         */
    }
}
